# 2026-09-11

- 「复盘计划」tab（`web/src/components/StatsPanel.vue`）顶部导出按钮配色调整：原来三个按钮统一用 `btn btn-sm btn-outline`（默认灰），现改为不同颜色 —— 导出交易明细 CSV → `btn-outline btn-primary`（紫 #5548ff）；导出归因统计 CSV → `btn-outline btn-info`（蓝 #3369ff）；导出每日快照 CSV → `btn-outline btn-success`（绿 #21a470）。刷新按钮保持 `btn-ghost`。注意：`web/src/style.css` 里全局 `.btn { color:#fff }` 只覆盖基础类，daisyUI 的 `.btn-outline.btn-*`（0,2,0 特异性）会胜出，所以描边按钮是「彩色文字 + 彩色边框 + 透明底」。
- 把项目改造成可安装的 PWA（不引入新依赖，纯手写方案）：
  - 新增 `web/public/manifest.webmanifest`（standalone、start_url/、maskable 图标、`?tab=stats` 快捷方式）、`web/public/icon.svg` + `icon-maskable.svg`，用 macOS `qlmanage -t -s 512` 从 SVG 渲染出 `icon-192.png` / `icon-512.png` / `icon-maskable-512.png` / `apple-touch-icon.png`（180）/ `favicon-32.png`。
  - 新增 `web/public/sw.js`：导航请求网络优先+离线回退 app shell；`/assets/*` 等静态资源缓存优先；`/api/*` 一律不接管（行情要实时）。带 SKIP_WAITING/controllerchange 更新机制。
  - `web/src/main.js` 仅在 `import.meta.env.PROD` 注册 SW（dev 不注册，避免干扰 HMR）。
  - 新增 `web/src/composables/usePwa.js`（beforeinstallprompt 捕获 + iOS 手动引导 + standalone 检测）；`AppHeader.vue` 加「📲 安装 App」按钮，`App.vue` 加 iOS 添加主屏图文引导、`canInstall` 透传。
  - `web/index.html` 补 manifest/apple-touch-icon/theme-color/description 等元信息；`useTheme.js` 在切换主题时同步 `meta[name=theme-color]`（深 #171717 / 浅 #ffffff）。
  - `server/server.js` serveStatic 增加 `.webmanifest`(application/manifest+json) 等 MIME；`/sw.js`、`/manifest.webmanifest`、`/index.html` 返回 `no-cache`，`/assets/*` 返回 immutable；`/sw.js` 带 `Service-Worker-Allowed: /`。
  - `App.vue` 页签支持 `?tab=stats` 直达复盘页（history.replaceState 同步），与 manifest 快捷方式配套。
  - 验证方式（已跑通后删除临时脚本）：`puppeteer-core` 起 headless Chrome + CDP `Page.getAppManifest` / `Page.getInstallabilityErrors`，结果 manifest errors `[]`、installabilityErrors `[]`、SW activated 且 controller=true。构建命令 `npm run build`，产物在 `dist/`。
  - 注意坑：用户当时已有一个旧 server 进程占着 3000 端口（内存里是旧代码），所以 `/manifest.webmanifest` 仍是 octet-stream，必须重启服务才能生效。
