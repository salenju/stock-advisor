- 持仓文件 `holdings.json` 以扁平 JSON 数组组织，每只标的独立对象，并通过 purchases/sells 子数组记录逐笔交易。
- 所有备份文件统一采用 `文件名-备份YYYYMMDD.json` 的命名约定，便于按日期回溯。
- 文档集中在 `docs/` 目录下按主题分文件（需求、技术设计、盈亏规则、项目计划、自测清单），不分散到代码文件中。
- AI 记忆文件按日期命名存放在 `.codebuddy/memory/`，与业务代码完全隔离。