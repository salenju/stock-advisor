运行入口 `node server/index.js` 默认启动 Web 服务（`127.0.0.1:3000`）+ 定时调度；设置 `ONCE=1` 仅执行一次行情抓取后退出。CLI 导入命令 `node server/import-csv.js [csv路径] [--create-missing]`，自动扫描 `data/` 下非 holdings 的 CSV 文件。配置位于仓库根目录 `config.json`，需包含 `schedule`、`notify`、`feishu`、`server` 等段。