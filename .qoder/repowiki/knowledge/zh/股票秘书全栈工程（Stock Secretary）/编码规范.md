- 所有可配置项集中放在根目录 config.json，server/ 与脚本通过读取该文件获取端口、调度间隔、飞书 webhook 等参数，避免硬编码。
- 前端源码位于 web/ 子目录，构建产物输出到根 dist/，由后端统一托管，保持前后端部署边界清晰。
- 开发时通过 vite.config.js 的 proxy 将 /api 请求代理到本地 :3000 后端，前后端联调无需跨域配置。
- CLI 脚本（start.sh、import-csv.js）统一使用带颜色的日志函数（log_info/log_warn/log_error/log_step）输出结构化信息。